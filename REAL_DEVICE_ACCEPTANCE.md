# FruitTreeScanner 真机验收表

评估日期：2026-07-02
关联文档：`COMMERCIAL_READINESS_REVIEW.md`

## 目的

这份文档用于把“App 看起来能跑”变成“真实设备、真实果园场景下可验收”。模拟器测试只能证明基础 UI、编译和部分逻辑没有明显问题；FruitTreeScanner 的商业价值来自 LiDAR、相机、定位、点云和估算链路，必须在支持 LiDAR 的真机上验证。

本验收表不新增功能，也不替代单元测试。它是每次准备试点、TestFlight 或客户演示前必须跑完的现场检查清单。

## 验收原则

1. 只验证当前主流程：新建扫描 → 真机扫描 → 停止录制 → 完成估算 → 保存记录 → 历史查看/导出。
2. 不把模拟器启动成功视为 LiDAR 扫描成功。
3. 每个失败项必须记录设备、系统版本、树体编号、操作步骤、截图/日志和是否可复现。
4. 阻塞项未通过时，不进入客户试点。
5. 低置信度估算不等于失败，但必须明确标记为需要复核。

## 设备矩阵

至少覆盖以下设备：

| 设备 | iOS/iPadOS | LiDAR | 用途 | 状态 |
|---|---|---:|---|---|
| iPhone Pro 系列 | 当前最低支持版本 | 是 | 手机现场主设备 | 待测 |
| iPad Pro | 当前最低支持版本 | 是 | 大屏采集/演示 | 待测 |
| 非 LiDAR iPhone | 当前最低支持版本 | 否 | readiness/阻断提示 | 待测 |

最低商业验收要求：

- 至少 1 台 LiDAR iPhone/iPad 完成全部 P0 项。
- 至少 1 台非 LiDAR 设备正确显示不可扫描提示，不能进入误导性扫描流程。

## 现场准备

验收前确认：

- App 为当前待验收构建。
- 相机权限、定位权限可被重置并重新授权。
- 电量 > 50%。
- 设备剩余存储 > 5GB。
- 果园或测试区域有至少 3 棵可重复扫描的树。
- 每棵树有唯一编号。
- 可导出文件到 Files/AirDrop 或本机共享入口。

## P0 阻塞验收

以下任一项失败，当前版本不得用于客户试点。

| ID | 场景 | 操作 | 通过标准 | 证据 |
|---|---|---|---|---|
| P0-01 | 首次启动 | 安装后第一次打开 App | 不崩溃，首页出现，主 CTA 明确 | 截图 |
| P0-02 | 相机权限拒绝 | 拒绝相机权限后进入扫描 | 显示明确阻断文案和返回/设置入口，不显示假扫描 UI | 截图 |
| P0-03 | 相机权限恢复 | 打开设置授权相机后回到 App | readiness 重新检查，允许继续扫描 | 录像 |
| P0-04 | LiDAR 不可用 | 在非 LiDAR 设备打开扫描 | 明确说明设备不支持，不进入采集 | 截图 |
| P0-05 | 定位拒绝 | 拒绝定位后扫描 | 扫描仍可进行；记录中 GPS 缺失被安全处理 | 记录详情 |
| P0-06 | 新建扫描 | 从首页发起新扫描 | 树体编号/扫描上下文明确，无空白危险状态 | 录像 |
| P0-07 | 点云增长 | 对同一棵树录制 30 秒 | 点云数量持续增长，无卡死/崩溃 | 录像 + HUD 截图 |
| P0-08 | 覆盖率反馈 | 绕树半圈到一圈 | 覆盖率或质量反馈能指导是否继续补扫 | 录像 |
| P0-09 | 停止录制 | 点击停止 | 进入可补扫/完成的后采集状态，不丢点云 | 截图 |
| P0-10 | 继续补扫 | 停止后继续录制 15 秒 | 原扫描上下文保留，点云继续增长 | 录像 |
| P0-11 | 完成估算 | 点击完成估算 | 显示处理中状态，完成后进入结果页 | 录像 |
| P0-12 | 结果持久化 | 估算完成后返回首页再进历史 | 记录存在，结果数值和点云入口存在 | 截图 |
| P0-13 | 点云打开 | 从历史打开刚生成点云 | 预览可打开，不崩溃 | 录像 |
| P0-14 | 文件导出 | 导出 PLY/CSV 或批量导出 | 外部工具可读取，字段无明显损坏 | 导出文件 |
| P0-15 | 后台切换 | 扫描中切到后台再回来 | 不崩溃；若扫描被系统中断，用户知道如何恢复 | 录像 |
| P0-16 | 估算中切后台 | 估算处理中切后台再回来 | 不重复估算，不丢记录，不停在假 loading | 录像 |
| P0-17 | 取消扫描 | 有点云时点击取消 | 有确认提示；确认放弃后不保存脏记录 | 录像 |
| P0-18 | 长录制 | 连续录制 120 秒 | 不崩溃，不明显内存爆涨，不出现 Metal/ARSession 致命错误 | 日志 |

## P1 质量验收

P1 失败不阻止内部试点，但必须进入下一轮修复计划。

| ID | 场景 | 通过标准 |
|---|---|---|
| P1-01 | 强光环境 | 扫描提示仍可读，结果页可判断质量 |
| P1-02 | 弱光环境 | App 能给出质量不足/需复核提示 |
| P1-03 | 树冠遮挡 | 低质量扫描不会给出过度自信结果 |
| P1-04 | 快速移动 | HUD 能提示移动过快或数据质量下降 |
| P1-05 | 低电量 | 不崩溃；性能下降时用户能恢复 |
| P1-06 | 横竖屏 | UI 不遮挡主操作，不出现不可点击按钮 |
| P1-07 | 历史数量增长 | 50 条记录下首页和历史页仍流畅 |
| P1-08 | 点云大文件 | 100 万点 PLY 可打开或有明确降级提示 |

## 每次验收记录模板

```text
验收日期：
App commit：
构建方式：Debug / Release / TestFlight
设备型号：
系统版本：
测试人员：
果园/地点：
天气/光照：

扫描树体：
- Tree ID：
- 品种：
- 预估树高：
- 遮挡情况：
- 扫描时长：
- 点云数量：
- 覆盖率：
- 估算结果：
- 是否保存：
- 是否导出：

失败项：
- ID：
- 复现步骤：
- 期望：
- 实际：
- 截图/录像/日志路径：
- 严重级别：P0 / P1 / P2
```

## 严重级别

| 级别 | 定义 | 处理 |
|---|---|---|
| P0 | 崩溃、丢数据、误导用户、无法完成主流程 | 阻止试点，必须修 |
| P1 | 主流程可完成，但体验或质量判断不稳定 | 进入下一轮修复 |
| P2 | 文案、布局、效率问题 | 排入普通 backlog |

## 发布门槛

客户试点前必须满足：

- 所有 P0 项通过。
- 至少 3 棵树完整扫描并能在历史中复查。
- 至少 1 次导出文件被外部工具打开。
- 估算结果页能明确显示置信度或复核建议。
- 已知限制写入试点说明，不隐藏算法边界。

## 不通过时的处理方式

发现失败后，不直接继续改随机代码。先补充以下信息：

1. 对应验收 ID。
2. 是否可稳定复现。
3. 是否阻塞主流程。
4. 失败发生在扫描、导出、估算、历史、权限还是设备环境。
5. 修复后需要重新跑哪些验收项。

只有确认属于 P0/P1 且有复现证据，才进入代码修复。
